// #include "location_q.cpp"
#pragma once
#include <vector>
#include <cmath>


// Function that updates the current direction vector to point in the direction of travel

void GetDirection(std::vector<double> &DirectionVector_Unit, std::vector<double> position, std::vector<double> q_goal);
