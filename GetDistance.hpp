#pragma once
#include <vector>
#include <cmath>
#include <iostream>
#include <windows.h>
using namespace std;


double GetDistance(std::vector<double> &curr_pos, std::vector<double> &destination);
